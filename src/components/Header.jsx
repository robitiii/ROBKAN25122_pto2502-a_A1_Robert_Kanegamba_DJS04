// src/components/Header.jsx

/**
 * Header component - Displays the main application header with title.
 * 
 * @component
 * @returns {JSX.Element} The rendered header element with podcast explorer title.
 * 
 * @example
 * <Header />
 */
function Header() {
  return (
    <header className="header">
      <h1>🎧 Podcast Explorer</h1>
    </header>
  );
}

export default Header;
